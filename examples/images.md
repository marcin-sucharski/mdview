# Local Images

This document references a local image file. In iTerm2, mdview emits the
inline image protocol. In other terminals, or through tmux without passthrough,
it falls back to a text line.

![A small generated sample](assets/sample.png)

The image path is resolved relative to this Markdown file.
